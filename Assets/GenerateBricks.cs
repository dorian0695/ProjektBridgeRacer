using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GenerateBricks : MonoBehaviour
{

    [SerializeField]
    GameObject blueBrick;
    [SerializeField]
    GameObject redBrick;
    [SerializeField]
    GameObject yellowBrick;
    //10 10 10

    // 6 transform.position.y 8 z
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
